package main

import "fmt"

const age = 90

var name = "godeez"

func main() {
	const name string = "deez"
	const age = 80
	fmt.Println(name)

	const (
		port = 5000
		host = "localhost"
	)

	// const sub string

	// sub = "sub"
}